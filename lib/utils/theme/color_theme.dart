import 'package:flutter/material.dart';

class DDSilverColors {
  // AppBar Text Color
  static const Color appBarTXT = Colors.white;
  static const Color appBarBG = Color(0xFF001F3F); // Navy Blue

  // Auth button Color
  static const Color buttonBGColor = Color(0xFF001F3F); // Navy Blue
  static const Color buttonTXTColor = Colors.white;
  static const Color linkTXTColor = Color(0xFF001F3F);
  static const Color authTXTRegular = Color(0xFF757575);

  // Auth Input Colors
  static const Color border = Color(0xFFA8A8A9);
  static const Color inputFill = Color(0xFFF3F3F3);
  static const Color inputHint = Color(0xFF676767);

  // Icons & Text
  static const Color iconGrey = Colors.grey; 
  static const Color textRegular = Color(0xFF757575);
  static const Color textHeading = Colors.black;
  
  // Primary Colors (Affects AppBar globally)
  static const Color primary = Color(0xFF001F3F); 
  static const Color accent = Color(0xFF001F3F); 

  //Shadows
  static const Color shadow = Colors.grey;

  // Neutral colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Colors.white;
  static const Color text = Color(0xFF212121);
  static const Color subtext = Color(0xFF676767);

  // Extra palette
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);

  // bottomButton Bar
  static const Color bottomBorder = Color(0xFFD8CFC8);
  static const Color bottomBG = Color(0xFFEFE8E0);

  // Rating Star Color
  static const Color ratingStar = Colors.amber;
}
